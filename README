install libs:
    pip install pyzbar
    pip install psutil
    pip install tendo
--    pip install watchdog
--    pip install quantumrandom


install dependencies:
    sudo apt-get install libzbar0
or
    brew install zbar
